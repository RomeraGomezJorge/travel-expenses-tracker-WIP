<form method="post" action="{{ path('location_costs_delete', {'id': location_cost.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location_cost.id) }}">
    <button class="btn">Delete</button>
</form>
