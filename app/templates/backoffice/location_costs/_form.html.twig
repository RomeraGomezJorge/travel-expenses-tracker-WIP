{{ form_start(form) }}

<div class="col-lg-12">
    {{ form_row(form.location) }}
    {{ form_row(form.cost) }}
</div>

<div class="card-action">
    {{ form_row(form.save) }}
</div>

{{ form_end(form) }}