{% extends 'base.html.twig' %}

{% block title %}LocationCosts{% endblock %}

{% block body %}
    <h1>LocationCosts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location_cost.id }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ location_cost.location }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ location_cost.cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('location_costs_index') }}">back to list</a>

    <a href="{{ path('location_costs_edit', {'id': location_cost.id}) }}">edit</a>

    {{ include('location_costs/_delete_form.html.twig') }}
{% endblock %}
