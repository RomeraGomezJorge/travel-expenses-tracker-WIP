{{ form_start(form) }}

<div class="col-lg-12">
    {{ form_row(form.name) }}
    {{ form_row(form.surname) }}
    {{ form_row(form.identityCard) }}
</div>

<div class="card-action">
    {{ form_row(form.save) }}
</div>

{{ form_end(form) }}


