<form method="post" action="{{ path('trip_destination_delete', {'id': trip_destination.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trip_destination.id) }}">
    <button class="btn">Delete</button>
</form>
