{% extends 'base.html.twig' %}

{% block title %}TripDestination{% endblock %}

{% block body %}
    <h1>TripDestination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trip_destination.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ trip_destination.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('trip_destination_index') }}">back to list</a>

    <a href="{{ path('trip_destination_edit', {'id': trip_destination.id}) }}">edit</a>

    {{ include('trip_destination/_delete_form.html.twig') }}
{% endblock %}
